// Email config placeholder — copy and fill in your EmailJS credentials.
// Get them from https://www.emailjs.com/ (free tier available).

export const EMAIL_SERVICE_ID = '';   // e.g. 'service_xxxxxxx'
export const EMAIL_TEMPLATE_ID = '';  // e.g. 'template_xxxxxxx'
export const EMAIL_PUBLIC_KEY = '';   // e.g. 'user_xxxxxxxxxxxxxxxxxxx'
